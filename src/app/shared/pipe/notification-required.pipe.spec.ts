import { NotificationRequiredPipe } from './notification-required.pipe';

describe('NotificationRequiredPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationRequiredPipe();
    expect(pipe).toBeTruthy();
  });
});
